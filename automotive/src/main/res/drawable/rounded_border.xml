<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- 카드 배경색을 흰색으로 설정 -->
    <corners android:radius="12dp" /> <!-- 테두리의 둥근 정도를 설정 -->
    <stroke android:color="@android:color/darker_gray" android:width="1dp"/> <!-- 테두리 색과 두께 설정 -->
    <padding android:left="8dp" android:right="8dp" android:top="8dp" android:bottom="8dp"/>
</shape>